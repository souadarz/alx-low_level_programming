#include <stdio.h>
#include <stdlib.h>

/**
 * main - the entry point to program that displays the information
 *        contained in the ELF header at the start of an ELF file.
 *Return: ...
 */

int main(void)
{
	exit(98);
	return (0);
}
